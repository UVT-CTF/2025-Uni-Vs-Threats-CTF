version: "3"
services:
  server:
    build: public
    container_name: BackroomsChallenge
    environment:
      - PORT=8080
      - FLAG="UVT{st0p_sn00p1n9_4r0und}"
    ports:
      - "57291:8080"
    restart: always
